# Versioned Model Artifacts Registry

Store trained ONNX and serialized classifier weights here. In serverless production environments (Vercel), heavy neural models run via external inference endpoints or deterministic rule fallbacks to respect serverless function size constraints.
